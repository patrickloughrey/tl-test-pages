#### Clone the repository
- `git clone https://github.com/patrickloughrey/tl-test-pages.git`

#### Install dependencies 
- `npm install`

#### Run the project
- `node app.js`

#### Navigate to the locally-hosted endpoint
- `localhost:3000/banner`
- `localhost:3000/native`